/** Mock hero stat for the left card on the course detail screen (reference layout). */
export const COURSE_HERO_MOCK_STAT = {
  percentLabel: "94%",
  caption:
    "consistent progress across graded components when weights and scores stay up to date.",
} as const;
