/** Rotating border ring thickness (px per side). */
export const GLASS_BORDER_GLOW_WIDTH = 2;

/** One full rotation of the silver border highlight. */
export const GLASS_BORDER_ROTATION_MS = 2800;

/** Shine sweep across the card face. */
export const GLASS_SHINE_SWEEP_MS = 1800;

/** Courses grid card hover lift. */
export const COURSE_GRID_HOVER_SCALE = 1.06;

/** Courses grid card press shrink. */
export const COURSE_GRID_PRESS_SCALE = 0.985;

/** Extra layout padding so 1.06 scale is not clipped in the grid. */
export const COURSE_GRID_SCALE_PADDING_RATIO = 0.03;
