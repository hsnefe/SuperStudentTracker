import { ScreenContainer, PlaceholderCard } from "@/components";

export function ScheduleScreen() {
  return (
    <ScreenContainer>
      <PlaceholderCard
        title="Weekly Schedule"
        description="Your fixed weekly timetable across courses."
        hint="Phase 1 — coming next: weekly grid, today highlight, slot editor."
      />
    </ScreenContainer>
  );
}
