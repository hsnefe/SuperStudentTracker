rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary baseline: authenticated users can read/write app data.
    // Tighten this with per-user ownership rules before production rollout.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
