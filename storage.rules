rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Course covers, material PDFs, and nested paths under courses/{uid}/...
    match /courses/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
